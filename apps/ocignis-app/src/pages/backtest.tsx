import { PageTitle } from 'common/utils';
import { BacktestPage } from 'modules';

const Backtest = () => (
  <PageTitle title="Backtest">
    <BacktestPage />
  </PageTitle>
);

export default Backtest;
